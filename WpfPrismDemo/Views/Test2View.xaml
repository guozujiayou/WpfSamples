<UserControl x:Class="WpfPrismDemo.Views.Test2View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfPrismDemo.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <WrapPanel>
            <TextBlock Text="{Binding Path=Count,StringFormat={}Count:{0}}" VerticalAlignment="Center" Margin="0 0 10 0"/>

            <Button Content="加" Command="{Binding AddCommand}"/>
        </WrapPanel>
    </Grid>
</UserControl>
